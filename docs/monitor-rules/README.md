# 需求监控规则版本记录

每次接入规则，保存原文快照、与上一上线版本的差异和验收结果。

## V0009 (2026-09-08)

- 原文快照见 `V0009.source.json`，来源仍为仓库根目录的需求总表字段监控归类.xlsx；保留 V0008 和此前归档。
- 新增“负责组员”列，39 个监控字段的组员映射与当前配置一致，已逐项核对；字段校验、阶段和排除类型未改变。
- 策划组长按“策划人员”精确分组：李东负责许翔宇、刘飞、刘宇、李海龙、汪永浩；时振兴负责陈知白、贾红亮、高文龙、李东、李猛。
- 多策划分别匹配两组并去重；未匹配策划、空策划不会默认归给时振兴，交由总兜底覆盖。李东本人的策划任务归时振兴兜底。
- 总兜底人为王谦、李晶晶、刘晓明。个人页保留本人组员责任和本职组长责任，不混入全量总兜底字段。
- 扫描结果保留组员/组长/总兜底来源，发送仍按同一收件人同一任务合并；H5 各视图使用对应字段集。
- M02 组员待办和兜底筛选从新规则派生策划分组，不修改其他模块共享的工作流配置。
- 实时总表 63 列，人员来源列均存在；工作日 289 个日期。“监修时间”仍缺列并安全跳过且记录日志。
- EA v0.7.12，页面/模块 v0.3.7，缓存 v24。覆盖原文映射、10 位策划分组、多成员、未匹配、重名近似、角色交叉、总兜底权限、缓存同步和快速刷新。
- 无新增定时器或外部请求；责任来源只增加少量字段元数据。刷新和访问日志记录 V0009、责任范围及计数，不发送真实测试通知。
- 上线验收：全量读取 3581 条记录并重建缓存 v24；8 位组长责任字段与兜底筛选逐任务一致。赵琛相关 237 条另行读取验证，按既有时间/项目范围排除后异常为 0；验收不再要求每位组长永远有非零待处理项。

## V0008 (2026-09-08)

- 来源原文见 `V0008.source.json`，保留 V0007 原文快照。
- 与 V0007 比较，仅五个 UI 字段的条件必填状态改变，其他字段、阶段、排除类型和职责不变。
- UI耗时、UI日方时间、UI剩余时间、UI开始时间、UI完成时间：新增“替换图已拆完”状态允许空值。
- UI日方时间：新增“日方制作中”状态允许空值。
- 实时 UI进度选项使用“制作完成（未监修或提交）”，原文使用英文括号；显式兼容两种写法，不模糊匹配其他状态。
- 已填值继续校验日期上限和验收阶段剩余值；“替换图已全返”“替换图部分返回”“替换图拆分中”“部分完成”仍须填写。
- 需求总表仍为 63 列、工作日 289 个日期；“监修时间”仍缺列并安全跳过。新增状态“替换图已拆完”已通过企业微信字段选项核实。
- EA v0.7.11，需求监控页面/模块 v0.3.6，缓存 v23。规则原文对齐、真实状态名称、近似状态不免检、空值/零值/已有值校验、快速刷新和组长/兜底一致性纳入验收。
- 当前原文一致性测试统一为 `scripts/check-dev-progress-rule-source.js`。

## V0007 (2026-09-08)

- 上一上线版本为 V0004。V0005/V0006 未独立上线，也没有完整历史文件，不补造版本内容。
- `V0004.applied.json` 保存升级前实际执行的配置；`V0007.source.json` 保存本次 Excel 两个工作表的非空行、原行号和 SHA256。
- 仍有 39 个监控字段，阶段范围、组长分工和工作日计算沿用同一服务端规则链路。
- 策划人员新增排除 `bug`；`配置bug` 仍检查。
- UI耗时、UI剩余时间、UI开始时间、UI完成时间：UI进度为 `制作完成(未监修或提交)`、`已监修`、`已提交`、`日方制作中` 时允许空值。
- UI日方时间：仅前三个状态允许空值，`日方制作中` 仍必填。
- 按原文“则不得为空”处理为条件必填；已有值继续执行日期/数值校验。UI需求为 `-` 等原有整字段排除继续生效。
- 配置/数值剩余在验收阶段不得大于 0 的规则仍存在。
- 延续用户单独确认：王谦和李晶晶共同作为总兜底人；起始阶段包含自身。
- 需求总表实时核对为 63 列，新增“策划配置类型”未列入监控；“监修时间”仍不存在，继续跳过并记录字段缺失。工作日子表 289 个日期，可正常读取。
- 回归覆盖源表字段范围/排除/分工对齐、条件空值免检、已填值异常、零值、各责任人共享判断和缓存升级。

## V0004

历史上线配置见 `V0004.applied.json`；接入摘要见 `../progress-plan.md` 的 v0.7.3 记录。
