每次返回结果的时候都先说一声  你好老板
先确认我的问题是不是和我们当前的线程主题相关，相关的继续推进，不相关的提醒我
编写一张的计划表，每一步操作之后更新该表，让我知道做到哪一步了
如果是编写工具或者网页，需要增加版本号显示
如果需要输出一个包分享给别人，在功能代码的文件夹内增加一个输出文件夹名称，例如OutPackage
是否有 bug
是否有边界情况遗漏
是否影响现有功能
是否有性能问题
需要很详细明确的log日志
关于代码和参数配置的一些路径，选择相对路径不能是绝对路径
需要重启进程才能更好的验证某个内容的时候，记得帮我重启进程。